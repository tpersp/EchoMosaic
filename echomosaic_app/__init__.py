"""EchoMosaic application package.

This package holds the new modular seams introduced during the
de-monolith refactor. The legacy top-level ``app.py`` remains the
compatibility entrypoint while runtime state and bootstrap helpers
move here incrementally.
"""

